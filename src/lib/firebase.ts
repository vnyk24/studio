// Firebase is not configured in this version.
// You will need to add your Firebase initialization logic here if you decide to use it.
import type { FirebaseApp } from 'firebase/app';
import type { Auth } from 'firebase/auth';

// In this version, app and auth are not initialized with Firebase.
const app: FirebaseApp | null = null;
const auth: Auth | null = null;

export { app, auth };
